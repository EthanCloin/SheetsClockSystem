const CLOCKING_CHOICES = ["Clock In", "Clock Out"]

const employeeMap = {
      email:0, name:1, role:2
    }

const jobMap = {
      productionLot:0, jobName:1, task:3
    }

const dataMap = {
  timestamp:0, emailAddress:1, displayName:2, action:3, displayJob:4, task:5, timeIn:6, timeOut:7, hoursWorked:8, empName:9, empPosition:10, jobName:11, jobNumber:12, rate:13, week:14, year:15
}
